apiVersion: v1
kind: Service
metadata:
  name: shopsmart-backend
  namespace: shopsmart
  labels:
    app: shopsmart-backend
    component: backend
spec:
  type: ClusterIP
  selector:
    app: shopsmart-backend
  ports:
    - name: http
      protocol: TCP
      port: 5001
      targetPort: 5001
