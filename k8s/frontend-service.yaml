apiVersion: v1
kind: Service
metadata:
  name: shopsmart-frontend
  namespace: shopsmart
  labels:
    app: shopsmart-frontend
    component: frontend
spec:
  type: LoadBalancer
  selector:
    app: shopsmart-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
